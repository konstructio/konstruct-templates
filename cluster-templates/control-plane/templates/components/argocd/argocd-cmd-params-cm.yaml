apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  labels:
    app.kubernetes.io/name: argocd-cmd-params-cm
    app.kubernetes.io/part-of: argocd
data:
  # TLS terminates at the Envoy gateway; argocd-server runs http internally.
  server.insecure: 'true'
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: argocd-application-controller
spec:
  # Must stay 1 unless ARGOCD_CONTROLLER_REPLICAS is raised to match —
  # mismatched shard count makes the controllers stomp each other
  # (wedged syncs, phantom hooks).
  replicas: 1
  template:
    spec:
      containers:
        - name: argocd-application-controller
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-repo-server
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: argocd-repo-server
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-server
spec:
  replicas: 3
  template:
    spec:
      securityContext:
        fsGroup: 999
      containers:
        - name: argocd-server
