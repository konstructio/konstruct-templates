{{- if not .Values.saas }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.workloadClusterName }}-external-dns
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: '30'
spec:
  project: {{ .Values.workloadClusterName }}
  source:
    repoURL: https://kubernetes-sigs.github.io/external-dns
    targetRevision: 1.20.0
    helm:
      releaseName: external-dns
      values: |
        serviceAccount:
          create: true
          name: external-dns
        provider: {{ .Values.externalDnsProviderName }}
        sources:
          - service
          - gateway-httproute
          - gateway-grpcroute
        # tls/tcp/udp route sources removed: gateway-api v1.4 serves TLSRoute
        # only at v1, external-dns 1.20 informers want v1alpha2 and crash.
        # Nothing on child clusters publishes those route kinds.
        domainFilters:
        - {{ .Values.domainName }}
        env:
        - name: {{ if eq .Values.externalDnsProviderName "civo" }}CIVO_TOKEN{{ else }}{{ .Values.externalDnsProviderTokenEnvName }}{{ end }}
          valueFrom:
            secretKeyRef:
              name: external-dns-secrets
              key: token
    chart: external-dns
  destination:
    name: {{ .Values.workloadClusterName }}
    namespace: external-dns
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: external-dns-gateway-{{ .Values.workloadClusterName }}
rules:
  - apiGroups: ["gateway.networking.k8s.io"]
    resources: ["gateways", "httproutes", "grpcroutes", "tlsroutes"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: external-dns-gateway-{{ .Values.workloadClusterName }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: external-dns-gateway
subjects:
  - kind: ServiceAccount
    name: external-dns
    namespace: external-dns
{{- end }}
