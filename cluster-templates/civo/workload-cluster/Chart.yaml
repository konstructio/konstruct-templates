apiVersion: v2
name: civo-workload-cluster
description: "Civo workload cluster template for Konstruct with ArgoCD Applications and core platform services. Set theme: true to provision it as a Theme cluster (Foreman single-click apps host with shared gateway networking, wildcard TLS/DNS, observability, and the Theme app registry)."
type: application
version: 2.0.0
appVersion: "2.0"
keywords:
  - argocd
  - theme
  - workload-cluster
  - gitops
  - crossplane
  - terraform
  - civo
maintainers:
  - name: Konstruct
