apiVersion: v2
name: civo-theme-cluster
description: "Civo Theme cluster template for Konstruct — extends the workload cluster with shared gateway networking, wildcard TLS/DNS, observability, and the Theme app registry"
type: application
version: 2.0.0
appVersion: "2.0"
keywords:
  - argocd
  - theme
  - workload-cluster
  - gitops
  - crossplane
  - terraform
  - civo
maintainers:
  - name: Konstruct
