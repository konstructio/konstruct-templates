apiVersion: v2
name: aws-workload-cluster
description: "AWS workload cluster template for Konstruct with ArgoCD Applications, Crossplane infrastructure, and core platform services. Set theme: true to provision it as a Theme cluster (Foreman single-click apps host with wildcard TLS/DNS, observability, and the Theme app registry)."
type: application
version: 2.0.0
appVersion: "2.0"
keywords:
  - argocd
  - theme
  - workload-cluster
  - gitops
  - crossplane
  - terraform
  - aws
maintainers:
  - name: Konstruct
