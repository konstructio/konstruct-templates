apiVersion: v2
name: aws-theme-cluster
description: "AWS Theme cluster template for Konstruct — extends the workload cluster with shared gateway networking, wildcard TLS/DNS, observability, and the Theme app registry"
type: application
version: 2.0.0
appVersion: "2.0"
keywords:
  - argocd
  - theme
  - workload-cluster
  - gitops
  - crossplane
  - terraform
  - aws
maintainers:
  - name: Konstruct
