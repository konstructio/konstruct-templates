apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.projectClusterName }}-secrets
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: '40'
spec:
  project: {{ .Values.projectClusterName }}
  source:
    repoURL: {{ .Values.gitopsRepoUrl }}
    path: registry/konstruct-clusters/{{ .Values.projectClusterName }}/secrets
    targetRevision: HEAD
  destination:
    name: {{ .Values.projectClusterName }}
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
