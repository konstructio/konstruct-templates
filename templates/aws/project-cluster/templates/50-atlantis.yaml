apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: atlantis-{{ .Values.workloadClusterName }}
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "50"
spec:
  project: {{ .Values.workloadClusterName }}
  source:
    repoURL: https://runatlantis.github.io/helm-charts
    chart: atlantis
    targetRevision: 6.9.3
    helm:
      values: |-
        statefulSet:
          annotations:
            secret.reloader.stakater.com/reload: "atlantis-secrets,atlantis-github-token"
        atlantisUrl: https://atlantis-{{ .Values.workloadClusterName }}.{{ .Values.domainName }}
        orgAllowlist: {{ .Values.atlantisAllowList | default (printf "github.com/%s/*" .Values.orgName) }}
        hidePrevPlanComments: true
        serviceAccount:
          create: true
          mount: true
          annotations:
            eks.amazonaws.com/role-arn: 'arn:aws:iam::{{ .Values.workloadClusterAccountId }}:role/atlantis-{{ .Values.workloadClusterName }}'
        resources:
          limits:
            cpu: 400m
            memory: 1Gi
          requests:
            cpu: 400m
            memory: 512Mi
        ingress:
          enabled: true
          annotations:
            {{ .Values.certManagerIssuerAnnotation1 }}
            {{ .Values.certManagerIssuerAnnotation2 }}
            {{ .Values.certManagerIssuerAnnotation3 }}
            {{ .Values.certManagerIssuerAnnotation4 }}
          path: /
          host: atlantis-{{ .Values.workloadClusterName }}.{{ .Values.domainName }}
          ingressClassName: "nginx"
          tls:
            - secretName: atlantis-tls
              hosts:
                - atlantis-{{ .Values.workloadClusterName }}.{{ .Values.domainName }}
        loadEnvFromSecrets:
          - atlantis-secrets
          # ATLANTIS_GH_TOKEN, refreshed every 45m by the git-token-refresher CronJob
          - atlantis-github-token
        repoConfig: |
          ---
          repos:
          - id: {{ .Values.atlantisAllowList | default (printf "github.com/%s/*" .Values.orgName) }}
            workflow: default
            allowed_overrides: [apply_requirements]
            apply_requirements: [mergeable]
            # instance-scoped repo config so this Atlantis does not contend
            # with other instances reading the default atlantis.yaml
            repo_config_file: atlantis-project-cluster.yaml
  destination:
    name: {{ .Values.workloadClusterName }}
    namespace: atlantis
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        maxDuration: 5m0s
        factor: 2
