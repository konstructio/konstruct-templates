apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: crossplane-provider
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: '20'
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.project }}
  source:
    repoURL: {{ .Values.gitopsRepoUrl }}
    path: registry/clusters/{{ .Values.clusterName }}/components/crossplane/provider
    targetRevision: HEAD
  destination:
    name: {{ .Values.clusterDestination }}
    namespace: crossplane-system
  # The workdir PVC's storageClassName is defaulted by the cluster and
  # immutable; inherit the live value so Replace passes immutability.
  ignoreDifferences:
    - kind: PersistentVolumeClaim
      jsonPointers:
        - /spec/storageClassName
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - Replace=true
      - RespectIgnoreDifferences=true
