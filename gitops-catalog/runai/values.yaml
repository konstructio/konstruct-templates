# Run.ai GPU Orchestration Platform Configuration
# Deploys Run.ai cluster agent via pre-rendered manifests (hook annotations
# stripped for ArgoCD compatibility). Prereqs app creates required namespaces
# at sync-wave 49, manifests app deploys Run.ai at sync-wave 50.

# @input.type: string
# @input.description: Cluster name identifier
# @input.required: true
# @input.default: civo-gpu
clusterName: civo-gpu

# @input.type: string
# @input.description: GitOps repository URL for ArgoCD sync
# @input.required: true
# @input.default: https://github.com/civonetes/demo-organization-gitops.git
gitopsRepoUrl: https://github.com/civonetes/demo-organization-gitops.git

# @input.type: string
# @input.description: Target cluster destination for ArgoCD
# @input.required: true
# @input.default: civo-gpu
clusterDestination: civo-gpu

# @input.type: string
# @input.description: ArgoCD project name
# @input.required: true
# @input.default: civo-gpu
project: civo-gpu

# @input.type: string
# @input.description: ArgoCD Application namespace (where app-of-apps lives)
# @input.required: true
# @input.default: civo-gpu-environment
argocdAppNamespace: civo-gpu-environment

# =============================================================================
# RUN.AI CLUSTER CONFIGURATION
# These values are used as token replacements in the rendered manifests.
# =============================================================================

# @input.type: string
# @input.description: Run.ai control plane URL (provided by Run.ai after account setup)
# @input.required: true
# @input.default: https://placeholder.runai.ai
clusterUrl: https://placeholder.runai.ai

# @input.type: string
# @input.description: Cluster UID (provided by Run.ai onboarding wizard)
# @input.required: true
# @input.default: demo-cluster-uid
clusterUid: demo-cluster-uid
